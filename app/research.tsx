import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import * as Linking from "expo-linking";
import { useRouter } from "expo-router";
import { useState } from "react";
import { Alert, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from "react-native";

import { DisclosureBanner, colors } from "@/components/case-ui";
import { ScreenContainer } from "@/components/screen-container";
import { ResearchItem, useCaseData } from "@/lib/case-store";

const categories: ResearchItem["category"][] = ["STATUTE", "COURT_RULE", "CASE_LAW", "GOVERNMENT", "PUBLIC_RECORD", "POLICY", "NEWS", "OTHER"];

export default function ResearchScreen() {
  const router = useRouter(); const { addResearch, research } = useCaseData(); const [question, setQuestion] = useState(""); const [category, setCategory] = useState<ResearchItem["category"]>("GOVERNMENT"); const [title, setTitle] = useState(""); const [publisher, setPublisher] = useState(""); const [url, setUrl] = useState(""); const [publicationDate, setPublicationDate] = useState(""); const [quotation, setQuotation] = useState(""); const [summary, setSummary] = useState("");
  function save() { if (!question.trim()) { Alert.alert("Add a research question", "Keep the issue as a neutral question until an authoritative source has been reviewed."); return; } addResearch({ question: question.trim(), category, title: title.trim() || "Source not yet reviewed", publisher: publisher.trim() || "Publisher not recorded", url: url.trim(), accessDate: "2026-08-19", publicationDate: publicationDate.trim(), quotation: quotation.trim(), summary: summary.trim() || "Research topic only. No source-backed summary has been added.", status: title.trim() || url.trim() ? "SOURCE_REVIEWED" : "TOPIC" }); setQuestion(""); setTitle(""); setPublisher(""); setUrl(""); setPublicationDate(""); setQuotation(""); setSummary(""); Alert.alert("Saved locally", "This item remains a research topic or source review. Confirm current authority and jurisdiction before relying on it."); }
  async function openSource(sourceUrl: string) { if (!sourceUrl) { Alert.alert("No URL recorded", "Add a direct official or public source URL before opening a source from this workspace."); return; } const supported = await Linking.canOpenURL(sourceUrl); if (supported) await Linking.openURL(sourceUrl); else Alert.alert("Cannot open this URL", "Check that the saved URL is complete and begins with https://."); }
  return <ScreenContainer edges={["top", "bottom", "left", "right"]} style={styles.container}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false}><TopBar title="Research notebook" onBack={() => router.back()} /><View style={styles.hero}><View style={styles.heroIcon}><MaterialIcons name="menu-book" size={25} color={colors.navy} /></View><Text style={styles.title}>Capture sources, not conclusions.</Text><Text style={styles.subtitle}>Save a neutral case question first. Research entries retain their publisher, URL, access date, and source excerpt so the user can revisit what actually supports the note.</Text></View><DisclosureBanner compact /><Text style={styles.label}>Case question</Text><TextInput value={question} onChangeText={setQuestion} placeholder="Example: What current public rule covers this request in this court?" placeholderTextColor="#8B97A5" multiline style={[styles.input, styles.textarea]} textAlignVertical="top" /><Text style={styles.label}>Research category</Text><ChipSelector items={categories} value={category} onChange={setCategory} /><Text style={styles.label}>Source title and publisher</Text><TextInput value={title} onChangeText={setTitle} placeholder="Source title" placeholderTextColor="#8B97A5" style={styles.input} /><TextInput value={publisher} onChangeText={setPublisher} placeholder="Government agency, court, publisher, or archive" placeholderTextColor="#8B97A5" style={styles.input} /><Text style={styles.label}>Public source URL and publication date</Text><TextInput value={url} onChangeText={setUrl} placeholder="https://..." placeholderTextColor="#8B97A5" style={styles.input} autoCapitalize="none" keyboardType="url" /><TextInput value={publicationDate} onChangeText={setPublicationDate} placeholder="Publication date, if known" placeholderTextColor="#8B97A5" style={styles.input} /><Text style={styles.label}>Exact relevant quotation and research summary</Text><TextInput value={quotation} onChangeText={setQuotation} placeholder="Quote only what you can revisit at the source location." placeholderTextColor="#8B97A5" multiline style={[styles.input, styles.textareaShort]} textAlignVertical="top" /><TextInput value={summary} onChangeText={setSummary} placeholder="Neutral source summary; identify limitations and open questions." placeholderTextColor="#8B97A5" multiline style={[styles.input, styles.textareaShort]} textAlignVertical="top" /><View style={styles.reminder}><MaterialIcons name="visibility" size={18} color={colors.gold} /><Text style={styles.reminderText}>This workspace does not verify current law or make legal conclusions. Confirm the source, jurisdiction, procedural posture, and any limiting authority before relying on research.</Text></View><TouchableOpacity onPress={save} activeOpacity={0.8} style={styles.saveButton}><Text style={styles.saveText}>Save research item</Text><MaterialIcons name="bookmark-add" size={18} color={colors.white} /></TouchableOpacity><Text style={styles.listTitle}>Saved research</Text>{research.length ? research.map((item) => <View key={item.id} style={styles.sourceCard}><View style={styles.sourceCardTop}><Text style={styles.sourceCategory}>{item.category.replaceAll("_", " ")}</Text><Text style={styles.sourceStatus}>{item.status.replaceAll("_", " ")}</Text></View><Text style={styles.sourceQuestion}>{item.question}</Text><Text style={styles.sourceTitle}>{item.title}</Text><Text style={styles.sourcePublisher}>{item.publisher} · Accessed {item.accessDate}</Text>{item.quotation ? <Text style={styles.quote}>“{item.quotation}”</Text> : null}<Text style={styles.summary}>{item.summary}</Text>{item.url ? <TouchableOpacity onPress={() => openSource(item.url)} style={styles.openButton}><MaterialIcons name="open-in-new" size={15} color={colors.navy} /><Text style={styles.openText}>Open public source</Text></TouchableOpacity> : null}</View>) : <View style={styles.empty}><MaterialIcons name="manage-search" size={22} color={colors.slate} /><Text style={styles.emptyText}>No research item has been saved. Start with a neutral question and add source details as you review them.</Text></View>}</ScrollView></ScreenContainer>;
}

function TopBar({ title, onBack }: { title: string; onBack: () => void }) { return <View style={styles.topRow}><TouchableOpacity onPress={onBack} style={styles.backButton} activeOpacity={0.7}><MaterialIcons name="arrow-back" size={21} color={colors.navy} /></TouchableOpacity><Text style={styles.topTitle}>{title}</Text><View style={styles.topSpacer} /></View>; }
function ChipSelector<T extends string>({ items, value, onChange }: { items: T[]; value: T; onChange: (item: T) => void }) { return <View style={styles.chips}>{items.map((item) => <TouchableOpacity key={item} onPress={() => onChange(item)} style={[styles.chip, item === value && styles.chipActive]}><Text style={[styles.chipText, item === value && styles.chipTextActive]}>{item.replaceAll("_", " ")}</Text></TouchableOpacity>)}</View>; }
const styles = StyleSheet.create({ container: { backgroundColor: colors.paper }, content: { padding: 20, paddingBottom: 34, gap: 11 }, topRow: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 4 }, backButton: { width: 39, height: 39, borderRadius: 13, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.white, justifyContent: "center", alignItems: "center" }, topTitle: { color: colors.navy, fontSize: 14, fontWeight: "800" }, topSpacer: { width: 39 }, hero: { gap: 7, marginTop: 7 }, heroIcon: { width: 50, height: 50, borderRadius: 17, justifyContent: "center", alignItems: "center", backgroundColor: colors.blue }, title: { color: colors.charcoal, fontSize: 25, lineHeight: 31, letterSpacing: -0.4, fontWeight: "800" }, subtitle: { color: colors.slate, fontSize: 13, lineHeight: 19 }, label: { marginTop: 7, color: colors.charcoal, fontSize: 13, fontWeight: "800" }, chips: { flexDirection: "row", flexWrap: "wrap", gap: 7 }, chip: { paddingHorizontal: 10, paddingVertical: 7, borderRadius: 999, borderColor: colors.border, borderWidth: 1, backgroundColor: colors.white }, chipActive: { backgroundColor: colors.navy, borderColor: colors.navy }, chipText: { color: colors.slate, fontSize: 10, fontWeight: "800" }, chipTextActive: { color: colors.white }, input: { minHeight: 47, borderRadius: 13, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.white, color: colors.charcoal, paddingHorizontal: 13, paddingVertical: 12, fontSize: 13, lineHeight: 18 }, textarea: { minHeight: 88 }, textareaShort: { minHeight: 70 }, reminder: { flexDirection: "row", gap: 8, alignItems: "flex-start", backgroundColor: "#FFF1D6", borderRadius: 14, borderWidth: 1, borderColor: "#F0D19D", padding: 12, marginTop: 4 }, reminderText: { flex: 1, color: colors.gold, fontSize: 12, lineHeight: 17, fontWeight: "700" }, saveButton: { minHeight: 52, borderRadius: 16, backgroundColor: colors.navy, justifyContent: "center", alignItems: "center", flexDirection: "row", gap: 8, marginTop: 4 }, saveText: { color: colors.white, fontSize: 14, fontWeight: "800" }, listTitle: { color: colors.charcoal, fontSize: 18, lineHeight: 24, fontWeight: "800", marginTop: 10 }, sourceCard: { borderWidth: 1, borderColor: colors.border, backgroundColor: colors.white, borderRadius: 16, padding: 13, gap: 6 }, sourceCardTop: { flexDirection: "row", justifyContent: "space-between", gap: 8 }, sourceCategory: { color: colors.navy, fontSize: 9, fontWeight: "800", letterSpacing: 0.5 }, sourceStatus: { color: colors.slate, fontSize: 9, fontWeight: "800", letterSpacing: 0.4 }, sourceQuestion: { color: colors.charcoal, fontSize: 14, lineHeight: 19, fontWeight: "800" }, sourceTitle: { color: colors.navy, fontSize: 12, lineHeight: 17, fontWeight: "800" }, sourcePublisher: { color: colors.slate, fontSize: 10, lineHeight: 15, fontWeight: "600" }, quote: { color: colors.charcoal, fontSize: 11, lineHeight: 16, fontStyle: "italic", backgroundColor: "#F2F5F7", padding: 8, borderRadius: 9 }, summary: { color: colors.slate, fontSize: 11, lineHeight: 16 }, openButton: { flexDirection: "row", gap: 5, alignItems: "center", alignSelf: "flex-start", paddingTop: 3 }, openText: { color: colors.navy, fontSize: 11, fontWeight: "800" }, empty: { flexDirection: "row", gap: 8, backgroundColor: colors.white, borderRadius: 15, borderWidth: 1, borderColor: colors.border, padding: 13, alignItems: "flex-start" }, emptyText: { color: colors.slate, fontSize: 12, lineHeight: 17, flex: 1 } });
