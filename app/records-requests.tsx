import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useRouter } from "expo-router";
import { useState } from "react";
import { Alert, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from "react-native";

import { DisclosureBanner, colors } from "@/components/case-ui";
import { ScreenContainer } from "@/components/screen-container";
import { discoveryCategories, useCaseData } from "@/lib/case-store";

export default function RecordsRequestsScreen() {
  const router = useRouter(); const { addRecordsRequest, recordsRequests, caseProfile } = useCaseData(); const [jurisdiction, setJurisdiction] = useState(caseProfile.jurisdiction); const [agency, setAgency] = useState(""); const [subject, setSubject] = useState(""); const [dateRange, setDateRange] = useState(""); const [categories, setCategories] = useState<string[]>([]); const [deadline, setDeadline] = useState("");
  function toggleCategory(category: string) { setCategories((previous) => previous.includes(category) ? previous.filter((item) => item !== category) : [...previous, category]); }
  function save() { if (!agency.trim() || !subject.trim() || !categories.length) { Alert.alert("Complete the request basics", "Add the agency, incident or subject, and at least one requested record category."); return; } addRecordsRequest({ jurisdiction: jurisdiction.trim() || "Jurisdiction not recorded", agency: agency.trim(), subject: subject.trim(), dateRange: dateRange.trim(), categories, requestDate: "", responseDeadline: deadline.trim(), referenceNumber: "", fees: "", status: "DRAFT" }); setAgency(""); setSubject(""); setDateRange(""); setCategories([]); setDeadline(""); Alert.alert("Draft saved locally", "This is a user-generated draft for review. It is not sent, filed, or submitted by Pro Se Compass."); }
  return <ScreenContainer edges={["top", "bottom", "left", "right"]} style={styles.container}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false}><View style={styles.topRow}><TouchableOpacity onPress={() => router.back()} style={styles.backButton}><MaterialIcons name="arrow-back" size={21} color={colors.navy} /></TouchableOpacity><Text style={styles.topTitle}>Records requests</Text><View style={styles.topSpacer} /></View><Text style={styles.title}>Build an editable request checklist.</Text><Text style={styles.subtitle}>Choose the public agency, subject, timeframe, and requested categories. The result is a user-generated draft that needs independent review before sending.</Text><DisclosureBanner compact /><Text style={styles.label}>Jurisdiction and agency</Text><TextInput value={jurisdiction} onChangeText={setJurisdiction} placeholder="Jurisdiction" placeholderTextColor="#8B97A5" style={styles.input} /><TextInput value={agency} onChangeText={setAgency} placeholder="Agency or public records office" placeholderTextColor="#8B97A5" style={styles.input} /><Text style={styles.label}>Incident or subject and date range</Text><TextInput value={subject} onChangeText={setSubject} placeholder="Incident, case, or subject description" placeholderTextColor="#8B97A5" style={styles.input} /><TextInput value={dateRange} onChangeText={setDateRange} placeholder="Date range to request" placeholderTextColor="#8B97A5" style={styles.input} /><Text style={styles.label}>Requested categories</Text><View style={styles.chips}>{discoveryCategories.map((category) => <TouchableOpacity key={category} onPress={() => toggleCategory(category)} style={[styles.chip, categories.includes(category) && styles.chipActive]}><Text style={[styles.chipText, categories.includes(category) && styles.chipTextActive]}>{category}</Text></TouchableOpacity>)}</View><Text style={styles.label}>Response deadline, if known</Text><TextInput value={deadline} onChangeText={setDeadline} placeholder="YYYY-MM-DD or statutory/agency date" placeholderTextColor="#8B97A5" style={styles.input} /><View style={styles.draftNotice}><MaterialIcons name="edit-document" size={19} color={colors.gold} /><Text style={styles.draftNoticeText}>Every saved request is labeled <Text style={styles.draftStrong}>DRAFT — REVIEW BEFORE SENDING</Text>. Pro Se Compass does not submit, sign, serve, or appeal it.</Text></View><TouchableOpacity onPress={save} style={styles.saveButton}><Text style={styles.saveText}>Save request draft</Text><MaterialIcons name="save" size={18} color={colors.white} /></TouchableOpacity><Text style={styles.listTitle}>Request tracker</Text>{recordsRequests.length ? recordsRequests.map((request) => <View key={request.id} style={styles.requestCard}><View style={styles.requestTop}><Text style={styles.requestStatus}>{request.status}</Text><Text style={styles.requestJurisdiction}>{request.jurisdiction}</Text></View><Text style={styles.requestAgency}>{request.agency}</Text><Text style={styles.requestSubject}>{request.subject}</Text><Text style={styles.requestDetail}>{request.categories.join(" · ")}</Text><Text style={styles.requestDetail}>Deadline: {request.responseDeadline || "Not recorded"} · Ref.: {request.referenceNumber || "Not recorded"}</Text></View>) : <View style={styles.empty}><MaterialIcons name="description" size={22} color={colors.slate} /><Text style={styles.emptyText}>No request draft is saved. This tracker will remain local until you choose to export or use a draft elsewhere.</Text></View>}</ScrollView></ScreenContainer>;
}
const styles = StyleSheet.create({ container: { backgroundColor: colors.paper }, content: { padding: 20, paddingBottom: 34, gap: 11 }, topRow: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 5 }, backButton: { width: 39, height: 39, borderRadius: 13, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.white, justifyContent: "center", alignItems: "center" }, topTitle: { color: colors.navy, fontSize: 14, fontWeight: "800" }, topSpacer: { width: 39 }, title: { color: colors.charcoal, fontSize: 25, lineHeight: 31, letterSpacing: -0.4, fontWeight: "800", marginTop: 6 }, subtitle: { color: colors.slate, fontSize: 13, lineHeight: 19 }, label: { marginTop: 8, color: colors.charcoal, fontSize: 13, fontWeight: "800" }, input: { minHeight: 47, borderRadius: 13, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.white, color: colors.charcoal, paddingHorizontal: 13, paddingVertical: 12, fontSize: 13, lineHeight: 18 }, chips: { flexDirection: "row", flexWrap: "wrap", gap: 7 }, chip: { paddingHorizontal: 10, paddingVertical: 7, borderRadius: 999, borderColor: colors.border, borderWidth: 1, backgroundColor: colors.white }, chipActive: { backgroundColor: colors.navy, borderColor: colors.navy }, chipText: { color: colors.slate, fontSize: 10, fontWeight: "800" }, chipTextActive: { color: colors.white }, draftNotice: { flexDirection: "row", gap: 8, alignItems: "flex-start", padding: 12, borderRadius: 14, backgroundColor: "#FFF1D6", borderColor: "#F0D19D", borderWidth: 1, marginTop: 4 }, draftNoticeText: { flex: 1, color: colors.gold, fontSize: 12, lineHeight: 17, fontWeight: "700" }, draftStrong: { fontWeight: "900" }, saveButton: { minHeight: 52, borderRadius: 16, backgroundColor: colors.navy, justifyContent: "center", alignItems: "center", flexDirection: "row", gap: 8, marginTop: 4 }, saveText: { color: colors.white, fontSize: 14, fontWeight: "800" }, listTitle: { color: colors.charcoal, fontSize: 18, lineHeight: 24, fontWeight: "800", marginTop: 10 }, requestCard: { backgroundColor: colors.white, borderWidth: 1, borderColor: colors.border, borderRadius: 16, padding: 13, gap: 5 }, requestTop: { flexDirection: "row", justifyContent: "space-between", gap: 8 }, requestStatus: { color: colors.gold, fontSize: 10, fontWeight: "800", letterSpacing: 0.5 }, requestJurisdiction: { color: colors.slate, fontSize: 10, fontWeight: "700", textAlign: "right", flex: 1 }, requestAgency: { color: colors.navy, fontSize: 13, fontWeight: "800" }, requestSubject: { color: colors.charcoal, fontSize: 12, lineHeight: 17, fontWeight: "700" }, requestDetail: { color: colors.slate, fontSize: 10, lineHeight: 15, fontWeight: "600" }, empty: { flexDirection: "row", gap: 8, backgroundColor: colors.white, borderRadius: 15, borderWidth: 1, borderColor: colors.border, padding: 13, alignItems: "flex-start" }, emptyText: { color: colors.slate, fontSize: 12, lineHeight: 17, flex: 1 } });
