import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useRouter } from "expo-router";
import { useState } from "react";
import { Alert, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from "react-native";

import { DisclosureBanner, colors } from "@/components/case-ui";
import { ScreenContainer } from "@/components/screen-container";
import { PersonProfile, useCaseData } from "@/lib/case-store";

const roles: PersonProfile["role"][] = ["DEFENDANT", "ATTORNEY", "PROSECUTOR", "JUDGE", "OFFICER", "WITNESS", "EXPERT", "INVESTIGATOR", "AGENCY", "OTHER"];

export default function PeopleScreen() {
  const router = useRouter(); const { peopleProfiles, records, addPerson } = useCaseData(); const [name, setName] = useState(""); const [role, setRole] = useState<PersonProfile["role"]>("WITNESS"); const [relationship, setRelationship] = useState(""); const [sourceNote, setSourceNote] = useState(""); const [openQuestion, setOpenQuestion] = useState(""); const [relatedIds, setRelatedIds] = useState<string[]>([]);
  function toggleRecord(id: string) { setRelatedIds((previous) => previous.includes(id) ? previous.filter((entry) => entry !== id) : [...previous, id]); }
  function save() { if (!name.trim() || !sourceNote.trim()) { Alert.alert("Add a name or role and source note", "Only add information you entered yourself or lawfully obtained from an authorized or public source, and describe the source for the profile."); return; } addPerson({ name: name.trim(), role, relationship: relationship.trim() || "Relationship to events not recorded", sourceNote: sourceNote.trim(), relatedRecordIds: relatedIds, openQuestion: openQuestion.trim() }); setName(""); setRelationship(""); setSourceNote(""); setOpenQuestion(""); setRelatedIds([]); }
  return <ScreenContainer edges={["top", "bottom", "left", "right"]} style={styles.container}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false}><View style={styles.topRow}><TouchableOpacity onPress={() => router.back()} style={styles.backButton}><MaterialIcons name="arrow-back" size={21} color={colors.navy} /></TouchableOpacity><Text style={styles.topTitle}>People workspace</Text><View style={styles.topSpacer} /></View><Text style={styles.title}>Profile only what you can source.</Text><Text style={styles.subtitle}>Keep relationship details, source notes, linked records, potential discrepancies, and questions together without seeking or exposing private information.</Text><DisclosureBanner compact /><View style={styles.safety}><MaterialIcons name="privacy-tip" size={20} color={colors.navy} /><Text style={styles.safetyText}>Do not use Pro Se Compass to obtain private information, stalk, harass, dox, or contact people unlawfully. Record only user-entered, public, or authorized material.</Text></View><Text style={styles.label}>Name or role label</Text><TextInput value={name} onChangeText={setName} placeholder="Name as recorded, or agency / role label" placeholderTextColor="#8B97A5" style={styles.input} /><Text style={styles.label}>Role</Text><View style={styles.chips}>{roles.map((item) => <TouchableOpacity key={item} onPress={() => setRole(item)} style={[styles.chip, role === item && styles.chipActive]}><Text style={[styles.chipText, role === item && styles.chipTextActive]}>{item}</Text></TouchableOpacity>)}</View><Text style={styles.label}>Known relationship and source note</Text><TextInput value={relationship} onChangeText={setRelationship} placeholder="Relationship to events, as reflected in source material" placeholderTextColor="#8B97A5" style={styles.input} /><TextInput value={sourceNote} onChangeText={setSourceNote} multiline placeholder="Source, location, and limitations for this profile" placeholderTextColor="#8B97A5" style={[styles.input, styles.textarea]} textAlignVertical="top" /><Text style={styles.label}>Related source records</Text><View style={styles.recordList}>{records.map((record) => <TouchableOpacity key={record.id} onPress={() => toggleRecord(record.id)} style={[styles.recordChoice, relatedIds.includes(record.id) && styles.recordChoiceActive]}><MaterialIcons name={relatedIds.includes(record.id) ? "check-circle" : "radio-button-unchecked"} size={18} color={relatedIds.includes(record.id) ? colors.teal : colors.slate} /><Text style={styles.recordName} numberOfLines={1}>{record.itemName}</Text></TouchableOpacity>)}</View><Text style={styles.label}>Open question</Text><TextInput value={openQuestion} onChangeText={setOpenQuestion} multiline placeholder="Neutral verification question, if any" placeholderTextColor="#8B97A5" style={[styles.input, styles.textareaShort]} textAlignVertical="top" /><TouchableOpacity onPress={save} style={styles.saveButton}><Text style={styles.saveText}>Save local profile</Text><MaterialIcons name="person-add" size={18} color={colors.white} /></TouchableOpacity><Text style={styles.listTitle}>People and roles</Text>{peopleProfiles.length ? peopleProfiles.map((person) => <View key={person.id} style={styles.personCard}><View style={styles.personTop}><Text style={styles.personRole}>{person.role}</Text><Text style={styles.personCount}>{person.relatedRecordIds.length} linked source{person.relatedRecordIds.length === 1 ? "" : "s"}</Text></View><Text style={styles.personName}>{person.name}</Text><Text style={styles.personDetail}>{person.relationship}</Text><Text style={styles.personSource}>{person.sourceNote}</Text>{person.openQuestion ? <Text style={styles.personQuestion}>Question: {person.openQuestion}</Text> : null}</View>) : <View style={styles.empty}><MaterialIcons name="groups" size={22} color={colors.slate} /><Text style={styles.emptyText}>No people profile has been added. Start with a source note, not a private-information search.</Text></View>}</ScrollView></ScreenContainer>;
}
const styles = StyleSheet.create({ container: { backgroundColor: colors.paper }, content: { padding: 20, paddingBottom: 34, gap: 11 }, topRow: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 5 }, backButton: { width: 39, height: 39, borderRadius: 13, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.white, justifyContent: "center", alignItems: "center" }, topTitle: { color: colors.navy, fontSize: 14, fontWeight: "800" }, topSpacer: { width: 39 }, title: { color: colors.charcoal, fontSize: 25, lineHeight: 31, letterSpacing: -0.4, fontWeight: "800", marginTop: 6 }, subtitle: { color: colors.slate, fontSize: 13, lineHeight: 19 }, safety: { flexDirection: "row", gap: 9, backgroundColor: colors.blue, borderColor: "#C7D9E8", borderWidth: 1, borderRadius: 15, padding: 13, alignItems: "flex-start" }, safetyText: { flex: 1, color: colors.navy, fontSize: 12, lineHeight: 17, fontWeight: "700" }, label: { marginTop: 8, color: colors.charcoal, fontSize: 13, fontWeight: "800" }, input: { minHeight: 47, borderRadius: 13, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.white, color: colors.charcoal, paddingHorizontal: 13, paddingVertical: 12, fontSize: 13, lineHeight: 18 }, textarea: { minHeight: 88 }, textareaShort: { minHeight: 70 }, chips: { flexDirection: "row", flexWrap: "wrap", gap: 7 }, chip: { paddingHorizontal: 10, paddingVertical: 7, borderRadius: 999, borderColor: colors.border, borderWidth: 1, backgroundColor: colors.white }, chipActive: { backgroundColor: colors.navy, borderColor: colors.navy }, chipText: { color: colors.slate, fontSize: 10, fontWeight: "800" }, chipTextActive: { color: colors.white }, recordList: { gap: 7 }, recordChoice: { flexDirection: "row", gap: 9, padding: 10, borderWidth: 1, borderColor: colors.border, borderRadius: 13, backgroundColor: colors.white, alignItems: "center" }, recordChoiceActive: { borderColor: "#B5E1D7", backgroundColor: "#DDF4EE" }, recordName: { color: colors.charcoal, fontSize: 12, fontWeight: "800", flex: 1 }, saveButton: { minHeight: 52, borderRadius: 16, backgroundColor: colors.navy, justifyContent: "center", alignItems: "center", flexDirection: "row", gap: 8, marginTop: 4 }, saveText: { color: colors.white, fontSize: 14, fontWeight: "800" }, listTitle: { color: colors.charcoal, fontSize: 18, lineHeight: 24, fontWeight: "800", marginTop: 10 }, personCard: { backgroundColor: colors.white, borderWidth: 1, borderColor: colors.border, borderRadius: 16, padding: 13, gap: 5 }, personTop: { flexDirection: "row", justifyContent: "space-between", gap: 8 }, personRole: { color: colors.navy, fontSize: 10, fontWeight: "800", letterSpacing: 0.5 }, personCount: { color: colors.slate, fontSize: 10, fontWeight: "700", textAlign: "right" }, personName: { color: colors.charcoal, fontSize: 14, fontWeight: "800" }, personDetail: { color: colors.slate, fontSize: 12, lineHeight: 17, fontWeight: "600" }, personSource: { color: colors.navy, fontSize: 11, lineHeight: 16, backgroundColor: colors.blue, padding: 8, borderRadius: 9 }, personQuestion: { color: colors.gold, fontSize: 11, lineHeight: 16, fontWeight: "800" }, empty: { flexDirection: "row", gap: 8, backgroundColor: colors.white, borderRadius: 15, borderWidth: 1, borderColor: colors.border, padding: 13, alignItems: "flex-start" }, emptyText: { color: colors.slate, fontSize: 12, lineHeight: 17, flex: 1 } });
